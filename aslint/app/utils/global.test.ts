import { Global } from './global';

describe('Utils', () => {

  describe('Global', () => {

    it('should indicate that class exists', () => {
      expect(Global).toBeDefined();
    });

  });
});
