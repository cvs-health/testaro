import { NODE_TYPE } from './nodeType';

describe('Constants', () => {

  describe('NODE_TYPE', () => {

    it('should indicate that class exists', () => {
      expect(NODE_TYPE).toBeDefined();
    });

  });
});
