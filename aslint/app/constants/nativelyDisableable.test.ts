import { NATIVELY_DISABLEABLE } from './nativelyDisableable';

describe('Constants', () => {

  describe('NATIVELY_DISABLEABLE', () => {

    it('should indicate that class exists', () => {
      expect(NATIVELY_DISABLEABLE).toBeDefined();
    });

  });
});
