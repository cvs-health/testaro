import { UNCLEAR_LINK_PHRASES_ENGLISH } from './unclearLinkPhrases';

describe('Constants', () => {

  describe('UNCLEAR_LINK_PHRASES_ENGLISH', () => {

    it('should indicate that class exists', () => {
      expect(UNCLEAR_LINK_PHRASES_ENGLISH).toBeDefined();
    });

  });
});
