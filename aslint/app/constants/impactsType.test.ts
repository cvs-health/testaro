import { IMPACTS_TYPE } from './impactsType';

describe('Constants', () => {

  describe('IMPACTS_TYPE', () => {

    it('should indicate that class exists', () => {
      expect(IMPACTS_TYPE).toBeDefined();
    });

  });
});
