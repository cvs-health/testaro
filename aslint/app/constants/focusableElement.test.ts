import { FOCUSABLE_ELEMENTS } from './focusableElements';

describe('Constants', () => {

  describe('FOCUSABLE_ELEMENTS', () => {

    it('should indicate that class exists', () => {
      expect(FOCUSABLE_ELEMENTS).toBeDefined();
    });

  });
});
