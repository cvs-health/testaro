import { CATEGORY_TYPE } from './categoryType';

describe('Constants', () => {

  describe('CATEGORY_TYPE', () => {

    it('should indicate that class exists', () => {
      expect(CATEGORY_TYPE).toBeDefined();
    });

  });
});
